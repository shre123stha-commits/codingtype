# dependencies
node_modules/

# builds
dist/

# local runtime data (backend keeps sessions on disk when no cloud link)
backend/data/*.json
!backend/data/.gitkeep

# env files — use the platform's env vars (Vercel / Render dashboards) instead
.env
frontend/.env
backend/.env

# misc
*.log
.DS_Store
